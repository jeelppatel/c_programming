#include<stdio.h>
	
	
	main(){
		
		float length, area;
		printf("Enter the length of the rectangle: ");
		scanf("%f",&length);
		area=length*length;
		printf("The area of the rectangle is: %f", area);
		
	}
